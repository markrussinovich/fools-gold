# Fool's Gold — pipeline dependencies.
# Python >= 3.11 (tomllib; tested on 3.12). CUDA GPU required for
# training/eval stages; the demo variant generator and judge smoke tests
# run on CPU.

torch>=2.3
transformers>=4.51
peft>=0.11
safetensors>=0.4
vllm>=0.8            # generation/eval workers (line_b1_eval, c18, battery);
                     # paper runs used 0.13.x — see docs/REPRODUCING.md on
                     # engine-version sensitivity of individual sampled draws
datasets>=2.19       # public benchmark fetch + eval loaders
huggingface_hub>=0.23
numpy>=1.26
pyyaml>=6.0
packaging>=23.0      # version gates (src/antiablit/modeladapter.py)
requests>=2.31       # Responses-API judge transport (scripts/gpt55.py)
openai>=1.40         # LLM judge client (scripts/gpt55.py)
lm-eval>=0.4.10      # REQUIRED: the B1 training ladder's GSM8K verdict gate
                     # (scripts/line_b1_gsm8k.sh) runs it, not just the
                     # optional MMLU/WMDP/IFEval battery (scripts/ops/run_lm_eval.sh).
                     # >=0.4.10: earlier gsm8k task defs use the legacy
                     # "gsm8k" dataset id, which fails offline against the
                     # openai/gsm8k cache from scripts/data/download_public.sh

# ---- optional --------------------------------------------------------------
# azure-identity>=1.17     # only for JUDGE_BACKEND=azure (Entra ID auth)
# anthropic>=0.34          # only for the opt-in judge fallback
#                          # (JUDGE_ANTHROPIC_FALLBACK=1)
# optuna>=3.6              # only for the heretic-style attack search
# heretic-llm==1.4.0       # only for scripts/line_b0_attack6.py /
#                          # attack11/attack12 (public abliteration recipes)
# scikit-learn>=1.4        # only for src/antiablit/probes.py (linear probes)
